# cloud gateway configs

spring:
 cloud:
  gateway:
   routes:
    - id: asset-service
      uri: lb://asset-service
      predicates: 
       - Path=/assets/**
      filters:
       StripPrefix=1 
      